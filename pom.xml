<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>ar.com.airdrop</groupId>
    <artifactId>Airdrop</artifactId>
    <packaging>jar</packaging>
    <version>1.0-SNAPSHOT</version>
    <name>Airdrop</name>
    <url>http://maven.apache.org</url>

    <properties>
        <!-- Spring version -->
        <spring.framework.version>5.2.19.RELEASE</spring.framework.version>
        <spring.context.version>5.2.21.RELEASE</spring.context.version>
        <spring.version>3.0.5.RELEASE</spring.version>

        <!-- java -->
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <!-- Spring 3 dependencies -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-core</artifactId>
            <version>${spring.framework.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
            <version>${spring.context.version}</version>
        </dependency>
    </dependencies>

    <build>
      <finalName>airdrop</finalName>
      <plugins>
          <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-assembly-plugin</artifactId>
              <version>3.3.0</version>
              <configuration>
                  <!-- Sin sufijo: el fat jar pisa al thin jar y queda un unico
                       target/airdrop.jar ejecutable con java -jar airdrop.jar -->
                  <appendAssemblyId>false</appendAssemblyId>
                  <archive>
                      <manifest>
                          <mainClass>ar.com.airdrop.Main</mainClass>
                      </manifest>
                  </archive>
                  <descriptorRefs>
                      <descriptorRef>jar-with-dependencies</descriptorRef>
                  </descriptorRefs>
              </configuration>
              <executions>
                  <execution>
                      <id>make-assembly</id>
                      <phase>package</phase>
                      <goals>
                          <goal>single</goal>
                      </goals>
                  </execution>
              </executions>
          </plugin>

          <!-- Copia airdrop.jar a la carpeta destino segun el SO (deploy.dir,
               definido por los perfiles de mas abajo) al hacer mvn install -->
          <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-antrun-plugin</artifactId>
              <version>3.1.0</version>
              <executions>
                  <execution>
                      <id>deploy-jar</id>
                      <phase>install</phase>
                      <goals>
                          <goal>run</goal>
                      </goals>
                      <configuration>
                          <target>
                              <mkdir dir="${deploy.dir}"/>
                              <copy file="${project.build.directory}/airdrop.jar"
                                    todir="${deploy.dir}" overwrite="true"/>
                              <echo message="airdrop.jar copiado a ${deploy.dir}"/>
                          </target>
                      </configuration>
                  </execution>
              </executions>
          </plugin>
      </plugins>
    </build>

    <profiles>
        <!-- Mac y Linux: macOS tambien matchea la familia 'unix' en Maven -->
        <profile>
            <id>deploy-unix</id>
            <activation>
                <os><family>unix</family></os>
            </activation>
            <properties>
                <deploy.dir>${user.home}/.scripts/javaApps</deploy.dir>
            </properties>
        </profile>

        <!-- Windows: se deja en el Escritorio -->
        <profile>
            <id>deploy-windows</id>
            <activation>
                <os><family>windows</family></os>
            </activation>
            <properties>
                <deploy.dir>${user.home}/Desktop</deploy.dir>
            </properties>
        </profile>
    </profiles>

</project>
